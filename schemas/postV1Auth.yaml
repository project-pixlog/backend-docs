type: object
required:
  - email
  - password
properties:
  email:
    description: Email do usuário.
    type: string
    format: email
  password:
    description: Senha do usuário.
    type: string
    format: password
